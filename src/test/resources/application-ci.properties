# Hérite de application-test.properties, surcharge juste la DB

# DB CI ? le postgres du docker-compose Jenkins
spring.datasource.url=jdbc:postgresql://postgres:5432/base-db_ci
spring.datasource.username=base
spring.datasource.password=base

spring.flyway.url=jdbc:postgresql://postgres:5432/base-db_ci
spring.flyway.user=base
spring.flyway.password=base

spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true

# Même config que test
spring.cache.type=none
spring.data.redis.repositories.enabled=false
app.jwt.secret=0123456789abcdef0123456789abcdef
app.jwt.expiration=3600000